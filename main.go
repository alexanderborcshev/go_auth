package main

import (
	"fmt"
)

// This project now contains an HTTP server under cmd/server.
// To run the API server: go run ./cmd/server
func main() {
	fmt.Println("Use: go run ./cmd/server to start the API server.")
}
